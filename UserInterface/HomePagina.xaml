<Page x:Class="ProjectbeheerUserInterface.HomePagina"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProjectbeheerUserInterface"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomePagina">

    <Grid>
        <DataGrid ItemsSource="{Binding Projecten}" AutoGenerateColumns="False" Name="DataGrid">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Titel" Binding="{Binding Titel}" />
                <DataGridTextColumn Header="StartDatum" Binding="{Binding StartDatum}" />
                <DataGridTextColumn Header="Beschrijving" Binding="{Binding Beschrijving}" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" />
                <DataGridTemplateColumn Header="Acties">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Meer info" 
                            Click="Meer_Info_Click" 
                            Tag="{Binding}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</Page>
